#include<stdio.h>
int main()
{
  char str[],char s[];
  int i,j,k,l;
  l=strlen(s);
  for(i=0;str[i+l-1];i++)
  {
      k=1;
      for(j=0;j<=l-1;j++)
      {
        if(str[k]!=s[j])
           break;
        k++;
      }
      if(j==1)
           return(1);
    }
    return(-1);
 }
